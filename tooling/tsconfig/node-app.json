{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Praxis — Node.js application (service)",
  "extends": "./base.json",
  "compilerOptions": {
    "composite": true,
    "types": ["node"],
    "noEmit": false
  }
}
