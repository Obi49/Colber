{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Colber — Node.js application (service)",
  "extends": "./base.json",
  "compilerOptions": {
    "composite": true,
    "types": ["node"],
    "noEmit": false
  }
}
