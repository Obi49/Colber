{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Praxis — Node.js library (publishable package)",
  "extends": "./base.json",
  "compilerOptions": {
    "composite": true,
    "types": ["node"]
  }
}
